# 教训记录：Discord 免艾特配置

## 日期
2026-02-06

## 问题背景
想让蛋黄在 Discord 群聊中不需要被 @就能自动回复消息。

## 踩过的坑

### ❌ 尝试 1：修改全局 messages.ackReactionScope
```json
{"messages": {"ackReactionScope": "group-all"}}
```
**结果**：失败。这个只是控制"已读回执"的反应范围，不是控制是否需要艾特才能回复。

### ❌ 尝试 2：仅配置服务器级别
```json
{"channels": {"discord": {"guilds": {"{guildId}": {"requireMention": false}}}}}
```
**结果**：失败。Discord 的权限控制非常严格，仅配置服务器级别是不够的。

### ❌ 尝试 3：全局 requireMention
```json
{"channels": {"discord": {"requireMention": false}}}
```
**结果**：失败。配置格式验证不通过。

## ✅ 正确的做法

必须**精确到频道级别**配置：

```json
{
  "channels": {
    "discord": {
      "guilds": {
        "{服务器ID}": {
          "channels": {
            "{频道ID}": {
              "allow": true,
              "requireMention": false
            }
          }
        }
      }
    }
  }
}
```

### 实际配置示例
```json
{
  "channels": {
    "discord": {
      "enabled": true,
      "token": "...",
      "groupPolicy": "allowlist",
      "guilds": {
        "1468994854624165952": {
          "channels": {
            "1468994862937538684": {
              "allow": true,
              "requireMention": false
            }
          }
        }
      }
    }
  }
}
```

## 关键结论

1. **Discord 权限严格**：OpenClaw 对 Discord 的集成有严格的权限控制
2. **必须到频道级**：不能只配服务器 ID，必须具体到频道 ID
3. **需要 allow: true**：除了 `requireMention: false`，还需要显式设置 `allow: true`
4. **groupPolicy 会变化**：配置后 `groupPolicy` 会从 `open` 变成 `allowlist`

## 获取 ID 的方法

1. Discord 设置 → 高级 → 开启"开发者模式"
2. 右键点击**服务器名称** → 复制服务器 ID (Guild ID)
3. 右键点击**频道名称** → 复制频道 ID (Channel ID)

## 相关文档

- 配置文件路径：`~/.openclaw/openclaw.json`
- 修改后需要重启 Gateway 才能生效

---

*记录者：蛋黄 🐱*

*文档生成时间：2026-02-06 01:40:00 (Asia/Shanghai)*
